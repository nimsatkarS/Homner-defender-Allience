<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Complaint Review</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #555;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="file"],
        select,
        textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        img {
            max-width: 100%;
            height: auto;
            border-radius: 4px;
            margin-bottom: 16px;
        }

        textarea {
            resize: vertical;
        }

        select {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Customer Complaint Review</h2>
        
        <!-- Customer Information (Read-only) -->
        <label for="customerName">Customer Name:</label>
        <input type="text" id="customerName" name="customerName" value="John Doe" readonly>
        
        <label for="customerIdProof">ID Proof:</label>
        <input type="text" id="customerIdProof" name="customerIdProof" value="123456789" readonly>
        
        <!-- Complaint Message (Read-only) -->
        <label for="complaintMessage">Incident Details:</label>
        <textarea id="complaintMessage" name="complaintMessage" rows="4" readonly>Customer behaved dishonestly during the transaction.</textarea>

        <!-- Review Rating -->
        <label for="reviewRating">Review Rating:</label>
        <select id="reviewRating" name="reviewRating" required>
            <option value="5">5 - Excellent</option>
            <option value="4">4 - Very Good</option>
            <option value="3">3 - Good</option>
            <option value="2">2 - Fair</option>
            <option value="1">1 - Poor</option>
        </select>

        <!-- Review Comments -->
        <label for="reviewComments">Additional Comments:</label>
        <textarea id="reviewComments" name="reviewComments" rows="4"></textarea>

        <!-- Submit Button -->
        <button type="submit" onclick="submitReview()">Submit Review</button>
    </div>

    <script>
        // Preview customer image
        document.getElementById('previewImage').addEventListener('error', function() {
            // Handle image loading error (e.g., replace with a placeholder)
            this.src = 'placeholder_image.jpg';
        });

        // Submit review (Add your own logic here)
        function submitReview() {
            alert('Review submitted successfully!');
            // Add your own logic for submitting the review, e.g., sending data to a server
        }
    </script>
</body>
</html>